import BoardItemList from './BoardItemList';
import BoardItemGrid from './BoardItemGrid';
import BoardItemScroll from './BoardItemScroll';

const structureComponents = {
  list: BoardItemList,
  grid: BoardItemGrid,
  scroll: BoardItemScroll,
  default: BoardItemList,
};

export default structureComponents;